{
  "name": "sniffing-game-api",
  "version": "1.0.0",
  "private": true,
  "description": "Render backend for storing Sniffing save point answers in Postgres.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "check": "node --check index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
